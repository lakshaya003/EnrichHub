# EnrichHub
Learner &amp; Organiser Web Portal 
Server-side application using Node.js and Express.js.
HTML — structures the web pages.
CSS — styles the user interface.
EJS — templating language used to dynamically generate HTML pages.
SQL — used to create and query the SQLite database.
SQLite — database system used for storing courses, bookings, and site settings.
